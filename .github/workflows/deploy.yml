name: Build for Manual Deploy

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci || npm install
      - run: npm run build
        env:
          VITE_API_URL: https://eopms.ytbhai.com/eopms-api

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
      - run: cd backend && composer install --no-dev --optimize-autoloader

      - uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: dist/

      - uses: actions/upload-artifact@v4
        with:
          name: backend-build
          path: backend/
          # keep config.php/db.json off — don't want to overwrite live server config
          # (composer's vendor/ folder IS included, that's the point)